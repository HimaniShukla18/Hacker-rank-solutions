#include <stdio.h>

int migratoryBirds(int arr[], int n) {
    int count[6] = {0}; // Bird types are 1 to 5

    // Count frequency of each bird type
    for (int i = 0; i < n; i++) {
        count[arr[i]]++;
    }

    // Find the bird type with max frequency and smallest ID
    int maxCount = 0;
    int birdType = 1;
    for (int i = 1; i <= 5; i++) {
        if (count[i] > maxCount) {
            maxCount = count[i];
            birdType = i;
        }
    }

    return birdType;
}

int main() {
    int n;
    scanf("%d", &n);

    int arr[n];
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    int result = migratoryBirds(arr, n);
    printf("%d\n", result);

    return 0;
}
