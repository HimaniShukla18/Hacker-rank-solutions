#include <stdio.h>

// Function to reverse an integer
int reverse(int num) {
    int rev = 0;
    while (num > 0) {
        rev = rev * 10 + num % 10;
        num /= 10;
    }
    return rev;
}

// Function to count beautiful days
int beautifulDays(int i, int j, int k) {
    int count = 0;
    for (int day = i; day <= j; day++) {
        int rev = reverse(day);
        int diff = day - rev;
        if (diff < 0) diff = -diff;
        if (diff % k == 0) {
            count++;
        }
    }
    return count;
}

int main() {
    int i, j, k;
    scanf("%d %d %d", &i, &j, &k);
    int result = beautifulDays(i, j, k);
    printf("%d\n", result);
    return 0;
}
